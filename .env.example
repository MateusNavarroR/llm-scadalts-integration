# =========================================
# SCADA Agent - Configuração de Ambiente
# =========================================
# Copie este arquivo para .env e ajuste os valores

# --- SCADA-LTS ---
SCADA_BASE_URL=http://localhost:8080/Scada-LTS
# URL para acesso via navegador (iframe no dashboard). 
# Se não definido, o sistema usará o SCADA_BASE_URL.
# Útil quando o backend acessa o SCADA via rede interna/container, mas o usuário acessa via navegador externo.
SCADA_DASHBOARD_URL=http://localhost:8080/Scada-LTS

# IMPORTANTE: Não deixe espaços em branco após o valor das credenciais!
SCADA_USER=admin
SCADA_PASSWORD=admin

# --- Anthropic API ---
# Obtenha sua chave em: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sua_chave_aqui

# --- Google Gemini API ---
# Obtenha sua chave em: https://aistudio.google.com/
GEMINI_API_KEY=sua_chave_gemini_aqui

# O sistema usará automaticamente a que estiver preenchida.
# Se ambas estiverem, o padrão pode ser configurado no código.

# --- Configuração dos Pontos (XIDs) ---
# Mapeie aqui os IDs do seu SCADA para os nomes lógicos do sistema

# POINT_FREQ2=DP_XXXXXX (Opcional)

# --- Configurações opcionais ---
# SAMPLE_RATE_HZ=1.0
# BUFFER_SIZE_SECONDS=300
